{
  "name": "go-group-imports-leobastiani",
  "displayName": "Go Group Imports",
  "description": "VSCode extension for separating imports in go files into three groups: stdlib, 3rdparty, own.",
  "version": "1.1.5",
  "publisher": "aleksandra",
  "icon": "assets/logo.png",
  "galleryBanner": {
    "color": "#FFE779",
    "theme": "light"
  },
  "engines": {
    "vscode": "^1.20.0"
  },
  "keywords": [
    "go",
    "imports",
    "group"
  ],
  "categories": [
    "Formatters"
  ],
  "activationEvents": [
    "onLanguage:go",
    "onCommand:extension.goGroupImports"
  ],
  "main": "./out/extension",
  "contributes": {
    "commands": [
      {
        "command": "extension.goGroupImports",
        "title": "Group imports"
      }
    ],
    "configuration": {
      "title": "GoGroupImports",
      "properties": {
        "groupImports.onSave": {
          "type": "boolean",
          "default": true,
          "description": "Automatically group imports on save."
        },
        "groupImports.strategy": {
          "type": "array",
          "description": "Strategy to group"
        },
        "groupImports.exclude": {
          "type": "array",
          "description": "Exclude based on regex or string"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "postinstall": "node ./node_modules/vscode/bin/install",
    "test": "npm run compile && node ./node_modules/vscode/bin/test"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "typescript": "^5.5.3",
    "vscode": "^1.1.21"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/beerose/go-group-imports-vscode.git"
  },
  "homepage": "https://github.com/beerose/go-group-imports-vscode/blob/master/README.md",
  "dependencies": {
    "@types/mocha": "^10.0.7",
    "mocha": "^10.6.0"
  }
}
